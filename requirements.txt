# Exact pins, not >=. requirements.txt previously used loose bounds and the
# venv silently drifted to an OLDER set than a bare system `pip install` would
# pick up — the two environments diverged (playwright 1.62.0 vs 1.61.0,
# pytest 8.3.4 vs 9.1.1, etc.) with no signal that they had. Pinned to the
# known-good versions this suite is verified against.
#
# pytest is pinned below 9: pytest_runtest_makereport in conftest.py uses the
# legacy @pytest.hookimpl(hookwrapper=True) / `outcome = yield` form, which
# pytest 9 deprecates. Migrate that hook to `wrapper=True` before bumping.
playwright==1.62.0
pytest==8.3.4
pytest-playwright==0.9.0
pytest-rerunfailures==15.0
allure-pytest==2.13.5
python-dotenv==1.0.1
openpyxl==3.1.5
pytest-base-url==2.1.0
pytest-xdist==3.6.1
pytest-json-report==1.5.0
pytest-timeout==2.3.1
